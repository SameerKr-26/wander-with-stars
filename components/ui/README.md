# Reusable UI primitives.

shadcn/ui components are added here on demand (`npx shadcn@latest add button`).
Nothing is installed up front — components arrive with the milestone that needs
them, styled against the tokens defined in Phase 2 (docs/DESIGN_SYSTEM.md).
